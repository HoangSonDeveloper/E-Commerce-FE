import { Fragment } from "react";

export default function DetailedLecLayout({children}) {
    // const metadata = {
    //     title: params,
    //     description: `Lecturer ${params} Detail Page`,
    //   }
    return (
        <body>{children}</body>
    )
  }